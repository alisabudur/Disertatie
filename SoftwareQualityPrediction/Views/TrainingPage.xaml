<Page x:Class="SoftwareQualityPrediction.Views.TrainingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SoftwareQualityPrediction.Views"
      xmlns:viewModels="clr-namespace:SoftwareQualityPrediction.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TrainingPage">

    <Page.Resources>
        <viewModels:TrainingViewModel x:Key="TrainingViewModel"/>
    </Page.Resources>

    <Grid DataContext="{StaticResource TrainingViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:UploadFileView
            DataContext="{Binding UploadFileViewModel}"
            Margin="10"
            Grid.Row="0"
            Grid.Column="0"/>
    </Grid>
</Page>
